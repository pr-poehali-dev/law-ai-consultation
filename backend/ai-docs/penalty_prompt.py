PENALTY_CALC_SYSTEM = (
    "Ты — точный юридический калькулятор неустойки (пеней) по российскому праву. "
    "Выполняй ТОЛЬКО арифметические расчёты строго по данным пользователя. "
    "Никаких советов, рекомендаций, оговорок — только цифры, формула, расчёт. "
    "Отвечай только на русском языке."
)

PENALTY_CALC_PROMPT = """Рассчитай сумму неустойки (пеней) по данным пользователя.

Формулы (применяй ТОЛЬКО тот вариант, который выбрал пользователь):
Вариант А (проценты от суммы долга): Сумма × Дни × Ставка%/100 (если ставка в год — делить на 365).
Вариант Б (ключевая ставка ЦБ РФ): Сумма × Ключевая ставка × Доля (1/300 или кратность) × Дни. Ставку применяй по выбранному методу: по периодам ЦБ / на конец периода / на сегодня / на дату.
Вариант В (твёрдая сумма в день): Сумма_в_день × Дни.

Правила дней: первый день — день, следующий за датой начала. Последний день — включительно дата окончания или дата расчёта.
Частичные оплаты / увеличения долга: разбить на интервалы, считать от остатка на каждом интервале, суммировать.
Ограничение: если указано и расчёт превышает — применить ограничение (только для договорной неустойки).

ФОРМАТ ОТВЕТА — строго следующие блоки, каждый с заголовком на отдельной строке:

[ШАПКА]
Кому: [если есть в контексте документа, иначе пропустить]
От кого: [если есть]

[ЗАГОЛОВОК]
РАСЧЁТ НЕУСТОЙКИ

[ИСХОДНЫЕ_ДАННЫЕ]
Сумма основного долга: {debt_rub} руб.
Период просрочки: с {date_from} по {date_to}
Количество дней просрочки: {days}
Тип расчёта: {rate_type_label}
{rate_details}
{cap_info}

[РАСЧЁТ]
Формула: {formula}
{periods_table_or_steps}

[ИТОГ]
Итоговая сумма неустойки: {total_rub} руб. ({total_words})
Правовое основание: {legal_basis}

[СНОСКА]
Суд вправе снизить неустойку по ст. 333 ГК РФ при явной несоразмерности последствиям нарушения обязательства. Расчёт является арифметическим; правовая оценка риска снижения требует консультации юриста.

Если данных не хватает — запроси недостающее. Не выдумывай данные, которых нет.

Данные для расчёта:
{calc_data}"""
